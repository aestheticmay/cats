//
//  APICaller.swift
//  cats
//
//  Created by Майя Калицева on 12.11.2022.
//

import UIKit

class APICaller {
    /*
     func fetchData(completion: @escaping (Result<[CatModel], Error>) -> Void, pagination: Bool = false) {
     DispatchQueue.global().asyncAfter(deadline: .now(), execute: {
     let originalData = [CatModel]()
     let newData = [CatModel]()
     completion(.success(pagination? newData : originalData))
     })
     }
     /*  DispatchQueue.global().asyncAfter(deadline: .now() + 1, execute: {
      let originalData = [CatModel]()
      let newData = [CatModel]()
      completion(.success(pagination? newData : originalData))
      })
      }*/
     */
}

